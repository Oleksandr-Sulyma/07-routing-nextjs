-# 📝 NoteHub (Next.js Advanced Routing)

Застосунок для створення, перегляду, редагування та видалення нотаток. Побудовано на **Next.js 15** з використанням **App Router**, **TanStack Query**, **TypeScript** та власного API.

Проєкт реалізує багатосторінкову структуру та використовує **SSR**, **prefetch**, а також просунуті техніки маршрутизації:  
**Паралельні маршрути**, **Перехоплюючі маршрути**, **Catch-all**.

---

## 🚀 Демо

🔗 Live Demo: https://07-routing-nextjs-hxcpef4nc-3280673s-projects.vercel.app/notes/filter/all  
🔗 GitHub Репозиторій: https://github.com/Oleksandr-Sulyma/07-routing-nextjs

---

## 🧩 Технології

- ⚛️ **Next.js 15 (App Router)**
- 🧠 **TypeScript**
- 🗄️ **@tanstack/react-query**
- 🎨 **CSS Modules**
- 📦 **axios**
- ⏳ **use-debounce**
- ☁️ **Vercel Deployment**
- 📦 **ESLint + Prettier**

---

## 📂 Структура Проєкту

Проєкт побудовано з використанням **App Router** з чітким розділенням на Серверні та Клієнтські
компоненти.

````bash
/
├── app/
│   ├── layout.tsx             # Глобальний layout (Header, Footer, Provider, Слот для @modal)
│   ├── loading.tsx            # Глобальний UI завантаження для SSR
│   ├── error.tsx              # Глобальна межа помилок
│   ├── not-found.tsx          # Спеціальна сторінка 404
│   ├── page.tsx               # Головна сторінка (/)
│   ├── @modal/                # Паралельний слот для модальних вікон
│   │   ├── default.tsx        # Заглушка для неактивного модального вікна
│   │   └── (.)notes/[id]/     # Перехоплення маршруту /notes/[id]
│   │           │      ├── page.tsx    
│   │           │      └── loading.tsx
│   │           └── default.tsx
│   └── notes/                 # Базовий маршрут: /notes
│       ├── filter/            # Маршрути для фільтрації нотаток
│       │   ├── @sidebar/      # Паралельний слот для меню тегів (SidebarNotes)
│       │   └── [...tag]/      # Catch-all маршрут для фільтрації за тегом (e.g., /filter/Work)
│       ├── Notes.client.tsx   # Клієнтська логіка списку нотаток, пошуку, CRUD
│       └── page.tsx           # Серверний компонент: prefetchQuery для /notes
├── components/                # Багаторазові компоненти
│   ├── Header/
│   ├── Footer/
│   ├── TanStackProvider/      # Провайдер для React Query
│   └── ... (NoteList, NoteForm, SearchBox та ін.)
├── hooks/                     # Кастомні хуки
│   └── useModalControl.ts
├── lib/                       # Службові функції
│   └── api.ts                 # Усі функції роботи з API (fetchNotes, fetchNoteById, CRUD)
├── types/                     # Глобальні типи
│   └── note.ts                # Інтерфейси Note, NoteFormValues та ін.
└── public/
``
---

## ⚙️ Налаштування Локально
### 1. Клонування репозиторію

```bash
git clone [https://github.com/Oleksandr-Sulyma/07-routing-nextjs]
````

---

### 2. Встановлення залежностей

```bash
npm install
```

---

### 3. Створення .env.local файлу (Конфігурація API)

У корені проєкту створіть файл .env.local і додайте свій токен доступу:

Фрагмент коду

```bash
# Змінна має починатися з NEXT_PUBLIC_
NEXT_PUBLIC_NOTEHUB_TOKEN=your_personal_token
👉 Увага: Токен використовується у функції lib/api.ts для аутентифікації.
```

---

### 4. Запуск локально

```bash
npm run dev

```

Відкрийте у браузері: http://localhost:3000

---

## 💡 Основна Функціональність

- ✅ Багатосторінковість: Маршрути /, /notes, /notes/filter/[...tag]..
- ✅ Паралельні Маршрути (@sidebar): Реалізовано меню фільтрації нотаток за тегами (Work, Personal, Todo тощо), яке змінює лише контент сторінки /notes/filter без її перезавантаження.
- ✅ Catch-all Маршрути ([...tag]): Використовуються для реалізації логіки фільтрації за будь-яким тегом, а також для маршруту /notes/filter/all.
- ✅ Перехоплюючі Маршрути (@modal): Реалізовано відкриття деталей нотатки (маршрут /notes/[id]) у модальному вікні, зберігаючи фонову сторінку (наприклад, сторінку фільтрації).
- ✅ Закриття Модального Вікна: Використовується router.back() для повернення на попередній маршрут.
- ✅ Обробка 404: Реалізовано кастомну сторінку not-found.tsx.
- ✅ Кешування даних: Використання TanStack Query для гідратації кешу та управління станом на клієнті.
- ✅ Пошук нотаток за текстом з debounce.
- ✅ Створення та видалення нотаток.
- ✅ Пагінація списку нотаток.
- ✅ Обробка станів завантаження (loading.tsx) та помилок (error.tsx).
- ✅ Типізація TypeScript в усіх компонентах та API-функціях.

---

## 🧑‍💻 Автор

Олександр Сулима

-🔗 GitHub Профіль: https://github.com/Oleksandr-Sulyma

---

## 📜 Ліцензія

Проєкт створений у навчальних цілях.
